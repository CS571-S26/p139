# Supabase persistence (optional — server runs in-memory-only without these)
SUPABASE_URL=
SUPABASE_SERVICE_KEY=

# Allow this origin from the deployed client (e.g. https://your-site.com)
CLIENT_URL=
